{
  "name": "@masumi-agent-messenger/spacetimedb",
  "version": "1.0.0",
  "private": true,
  "description": "Encrypted agent-to-agent inbox SpacetimeDB module (Rust). The actual module sources live next to this file as a Cargo crate; the package.json is kept to participate in the pnpm workspace.",
  "scripts": {
    "check": "cargo check --target wasm32-unknown-unknown",
    "build": "spacetime build",
    "publish": "spacetime publish"
  }
}
