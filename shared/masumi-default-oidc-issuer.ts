/**
 * Default Masumi SaaS dev issuer when `MASUMI_OIDC_ISSUER` is unset.
 * Trailing slashes are stripped at call sites; do not include one here.
 */
export const MASUMI_DEFAULT_OIDC_ISSUER =
  'https://masumi-saas-dev-exyyd.ondigitalocean.app';
